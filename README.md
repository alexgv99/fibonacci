
# Instructions

## to generate the build use all dev-dependencies

```bash
rm -dfr node_modules
rm -dfr build
yarn
yarn build
```

## to serve use just the prod dependencies

```bash
rm -dfr node_modules
yarn serve
```
